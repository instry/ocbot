diff --git a/chrome/browser/ui/ocbot/ocbot_side_panel_manager.cc b/chrome/browser/ui/ocbot/ocbot_side_panel_manager.cc
index 3ed0fd492a553f6a87d7df217364d5e39223888a..dbc5cecf1c855f8adf84165818e860793c3f398c 100644
--- a/chrome/browser/ui/ocbot/ocbot_side_panel_manager.cc
+++ b/chrome/browser/ui/ocbot/ocbot_side_panel_manager.cc
@@ -33,17 +33,16 @@ void ToggleSidePanel(BrowserWindowInterface* browser_window) {
     return;
   }
 
-  // Check if the extension is installed and enabled
   auto* profile = browser_window->GetProfile();
   if (!profile) {
     return;
   }
-  
+
   auto* registry = extensions::ExtensionRegistry::Get(profile);
   if (!registry) {
     return;
   }
-  
+
   if (!registry->enabled_extensions().GetByID(kOcbotExtensionId)) {
     return;
   }
@@ -63,17 +62,16 @@ void OpenSidePanel(BrowserWindowInterface* browser_window) {
     return;
   }
 
-  // Check if the extension is installed and enabled
   auto* profile = browser_window->GetProfile();
   if (!profile) {
     return;
   }
-  
+
   auto* registry = extensions::ExtensionRegistry::Get(profile);
   if (!registry) {
     return;
   }
-  
+
   if (!registry->enabled_extensions().GetByID(kOcbotExtensionId)) {
     return;
   }
